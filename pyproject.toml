[project]
name = "python-programing-conways-game"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Neha Sharma", email = "neha28.sharma1@gmail.com" }
]
requires-python = ">=3.13"
dependencies = [
    "ruff>=0.16.8",
    "typer>=0.27.2",
]

[project.scripts]
python-programing-conways-game = "python_programing_conways_game:main"

[build-system]
requires = ["uv_build>=0.12.7,<0.13.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "pytest>=9.1.1",
]
